import { complited } from 'brain/helpers/complited';
import { module, test } from 'qunit';

module('Unit | Helper | complited');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = complited([42]);
  assert.ok(result);
});
