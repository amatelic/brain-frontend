import { arrayChange } from 'brain/helpers/array-change';
import { module, test } from 'qunit';

module('Unit | Helper | array change');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = arrayChange([42]);
  assert.ok(result);
});
