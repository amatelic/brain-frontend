import timeformatter from 'brain/utils/timeformatter';
import { module, test } from 'qunit';

module('Unit | Utility | timeformatter');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = timeformatter();
  assert.ok(result);
});
