<div class="header">
  <div class="brain__logo">
    <h2>Brain</h2>
  </div>
  <header>
      <div class="brain__navigation">
        {{#if session.isAuthenticated}}
        {{#user-conf user=model.user}}
          {{#link-to "user" class="btn"}} {{fa-icon "user"}} User {{/link-to}}
          {{#link-to "messages" class="btn"}} {{fa-icon "comments"}} Messages {{/link-to}}
          <a href="#" class="btn" {{action 'logout'}}>Logout</a>
        {{/user-conf}}
        {{else}}
          <div class="brain__navigation__component">
            {{#link-to "login" class="btn"}} Log in {{/link-to}}
            {{#link-to "register" class="btn"}} Register {{/link-to}}
          </div>
        {{/if}}
      </div>
  </header>
</div>

<div class="app">
  {{#if session.isAuthenticated}}
    <div class="left">
      <ul class="brain__nav">
        <li>{{fa-icon "home"}}{{#link-to "home" class="brain__links"}}Home{{/link-to}}</li>
        <li>{{fa-icon "tasks"}}{{#link-to "tasks" class="brain__links"}}Tasks{{/link-to}}</li>
        <li>{{fa-icon "calendar"}}{{#link-to "calender" class="brain__links"}}Calender{{/link-to}}</li>
        <li>{{fa-icon "line-chart"}}{{#link-to "graph" class="brain__links"}}Graph{{/link-to}}</li>
        <!-- <li>{{#link-to "photos.edit" photo}}Add{{/link-to}}</li> -->
      </ul>
    </div>
    <div class="main">
      {{liquid-outlet class="brain__main"}}
    </div>
  {{else}}
    {{outlet}}
  {{/if}}
</div>
<!-- <button {{action 'openModal' 'modal' 'test'}}>Open modal</button> -->
<!-- <button {{action 'openModal' 'modal.welcome' 'test'}}>Open welcome</button> -->
{{outlet 'modal'}}
<!-- {{#if showModal}}
  {{brain-modal complited="complited"}}
{{/if}} -->
